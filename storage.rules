rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /reports/{uid}/{reportId}.pdf {
      allow read: if request.auth != null && request.auth.uid == uid;
      allow write: if false;
    }
  }
}
